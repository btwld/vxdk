export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},

    // Allows stylized scrollbars with unprefixed selectors.
    'postcss-scrollbar': {},

    // Allows stylized input ranges with unprefixed selectors.
    'postcss-input-range': {},
  },
};
